# Information-security baseline

Multi-factor authentication (MFA) is required for email, source control, and
the virtual private network (VPN). Use the approved authenticator application;
SMS is permitted only as a temporary recovery method.

Report a suspected phishing message through the mail client. Do not share a
VPN account, an MFA code, or a password with a colleague or support agent.
